import React from 'react';

import Layout from '../components/Layout';

const PageNotFound = () => {
  return (
    <Layout>
      <p>page not found</p>
    </Layout>
  );
};

export default PageNotFound;
